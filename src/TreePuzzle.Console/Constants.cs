namespace TreePuzzle.Console;

public static class Constants
{
    public const int NUM_OF_SPACES_FOR_INDENT = 2;
    public const string DEFAULT_INPUT = "(id, name, email, type(id, name, customFields(c1, c2, c3)), externalId)";
}